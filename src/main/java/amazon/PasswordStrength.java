package amazon;

public class PasswordStrength {

    public static void main(String[] args) {
        String password = "good";
        //count distinct chars in substring
    }

    public static long getStrength(String password){
        long count = 0;
        for(int i = 0; i < password.length(); ++i)
            count += getCount()
    }
}
